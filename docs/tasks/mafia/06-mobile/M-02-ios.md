# M-02 — Swift Generated Client

Status: TODO
Depends: API-15, M-01

## Goal
OpenAPI → Swift generation pipeline: models + async API layer from the canonical spec,
regeneration wired into CI so mobile never drifts from the contract.

## Acceptance
- [ ] Sample flow: auth (M-01) + state sync + action against /v1
